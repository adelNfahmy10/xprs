import { Component, CUSTOM_ELEMENTS_SCHEMA } from '@angular/core'

@Component({
    selector: 'agent-profile-card',
    imports: [],
    templateUrl: './profile-card.component.html',
    schemas: [CUSTOM_ELEMENTS_SCHEMA]
})
export class ProfileCardComponent {}
